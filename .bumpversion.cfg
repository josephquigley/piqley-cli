[bumpversion]
current_version = 1.0.0
commit = True
tag = True
tag_name = v{new_version}
message = chore: bump version {current_version} → {new_version}

[bumpversion:file:Sources/piqley/Piqley.swift]
search = version: "{current_version}"
replace = version: "{new_version}"
