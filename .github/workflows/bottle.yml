name: Build Homebrew Bottle

on:
  release:
    types: [published]

jobs:
  bottle:
    strategy:
      matrix:
        os: [macos-15]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@master

      - name: Tap and install from source
        run: |
          brew tap quigs/tools "${{ github.workspace }}"
          brew install --build-bottle quigs/tools/quigsphoto-uploader

      - name: Create bottle
        run: |
          cd ~
          brew bottle --json --root-url="https://github.com/josephquigley/quigsphoto-uploader/releases/download/${{ github.event.release.tag_name }}" quigs/tools/quigsphoto-uploader

      - name: Upload bottle to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cd ~
          gh release upload "${{ github.event.release.tag_name }}" quigsphoto-uploader--*.tar.gz
          echo "Bottle uploaded. Update formula with the following JSON:"
          cat quigsphoto-uploader--*.json
