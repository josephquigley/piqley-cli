import Testing
@testable import piqley

@Suite("AppConfig")
struct ConfigTests {
    @Test("placeholder — rewritten in Task 3")
    func testPlaceholder() {
        // Will be rewritten in Task 3
    }
}
