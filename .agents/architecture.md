# piqley-cli — Architecture Guidelines

> The canonical architecture guidelines (no-magic-strings policy, enum patterns) live in the top-level `../../.agents/architecture.md`. This file is kept as a pointer — refer to the top-level version for the full policy.
