.Dd March 18, 2026
.Dt PIQLEY 1
.Os
.Sh NAME
.Nm piqley
.Nd process and publish photos to Ghost CMS
.Sh SYNOPSIS
.Nm
.Cm process
.Ar folder-path
.Op Fl -dry-run
.Op Fl -delete-source-contents
.Op Fl -delete-source-folder
.Op Fl -non-interactive
.Nm
.Cm setup
.Nm
.Cm config
.Nm
.Cm clear-cache
.Op Fl -plugin Ar name
.Nm
.Cm secret set
.Ar plugin key
.Nm
.Cm secret delete
.Ar plugin key
.Nm
.Cm plugin setup
.Op Ar plugin-name
.Op Fl -force
.Nm
.Cm plugin init
.Op Ar plugin-name
.Op Fl -no-examples
.Op Fl -non-interactive
.Nm
.Cm plugin config
.Ar plugin-name
.Nm
.Cm uninstall
.Op Fl -force
.Sh DESCRIPTION
.Nm
is a plugin-driven command-line tool for processing and publishing photos
to Ghost CMS as part of a Lightroom \(-> Hazel \(-> Ghost workflow.
.Pp
Given a folder of exported JPEG images,
.Nm
copies them into a temporary working directory and runs them through a
configurable plugin pipeline.
Plugins never have access to the original files and cannot modify or
destroy them.
The temporary directory is automatically cleaned up when processing
finishes.
.Pp
The plugin pipeline is defined in
.Pa ~/.config/piqley/config.json
and consists of named hooks that plugins can subscribe to.
Each plugin declares its hooks in a
.Pa manifest.json
and may provide executable commands, declarative metadata-matching rules
in
.Pa config.json ,
or both.
.Pp
Plugins are discovered automatically from
.Pa ~/.config/piqley/plugins/ .
Each plugin lives in its own subdirectory and contains a
.Pa manifest.json
(identity, hooks, config schema) and an optional
.Pa config.json
(runtime values and declarative rules).
.Pp
Only one instance of
.Nm
may run at a time.
An advisory file lock is used; if another instance is detected, the tool
exits immediately with an error.
.Sh COMMANDS
.Ss process folder-path
Scan
.Ar folder-path
for exported JPEG images and run them through the plugin pipeline.
.Pp
Available flags:
.Bl -tag -width Ds
.It Fl -dry-run
Preview all actions without uploading or sending anything.
.It Fl -delete-source-contents
Delete the contents of the source folder after a successful run.
The folder itself is preserved but all files and subdirectories inside it
are removed.
Has no effect during a dry run.
.It Fl -delete-source-folder
Delete the source folder and its contents after a successful run.
The entire directory is removed from disk.
Has no effect during a dry run.
.It Fl -non-interactive
Skip interactive prompts; drop invalid rules with warnings instead of
asking for confirmation.
.El
.Ss setup
Interactive configuration wizard.
Prompts for core settings and writes them to
.Pa ~/.config/piqley/config.json .
Installs bundled plugins from the distribution into the plugins
directory and runs plugin setup for all discovered plugins.
.Pp
Run this command before using
.Cm process
for the first time, or to update an existing configuration.
.Ss config
Open the main configuration file
.Pa ( ~/.config/piqley/config.json )
in the editor specified by the
.Ev EDITOR
environment variable.
On macOS, falls back to the system
.Xr open 1
command if
.Ev EDITOR
is not set.
.Ss clear-cache
Delete plugin execution logs.
.Pp
Available options:
.Bl -tag -width Ds
.It Fl -plugin Ar name
Clear only the specified plugin's execution log.
.El
.Pp
When
.Fl -plugin
is not specified, execution logs for all plugins are deleted.
.Ss secret set plugin key
Store a plugin secret.
Prompts for the value on standard input.
.Pp
On macOS, secrets are stored in the system Keychain.
On Linux, secrets are written to
.Pa ~/.config/piqley/secrets.json .
.Ss secret delete plugin key
Remove a previously stored plugin secret.
.Ss plugin setup Op plugin-name
Run interactive setup for one or all plugins.
Each plugin's manifest declares its configuration schema (plain values
and secrets); the setup scanner walks through each entry and prompts for
a value.
.Pp
Available flags:
.Bl -tag -width Ds
.It Fl -force
Force re-setup by clearing existing config values and the
.Li isSetUp
flag before prompting.
.El
.Pp
When
.Ar plugin-name
is omitted, setup runs for all discovered plugins.
.Ss plugin init Op plugin-name
Create a new declarative-only plugin.
Generates a skeleton plugin directory under
.Pa ~/.config/piqley/plugins/
with a
.Pa manifest.json
and
.Pa config.json
pre-populated with example hooks and rules.
.Pp
Available flags:
.Bl -tag -width Ds
.It Fl -no-examples
Skip example rules and values in the generated config files.
.It Fl -non-interactive
Non-interactive mode.
Requires the
.Ar plugin-name
argument.
.El
.Pp
When
.Ar plugin-name
is omitted in interactive mode, the command prompts for a name.
.Ss plugin config plugin-name
Open the specified plugin's
.Pa config.json
in the editor specified by the
.Ev EDITOR
environment variable.
On macOS, falls back to the system
.Xr open 1
command if
.Ev EDITOR
is not set.
.Ss uninstall
Remove all piqley configuration, plugins, and stored secrets.
Deletes the
.Pa ~/.config/piqley/
directory and, on macOS, all Keychain items for the
.Nm
service.
.Pp
Available flags:
.Bl -tag -width Ds
.It Fl -force
Skip the confirmation prompt.
.El
.Sh CONFIGURATION
Non-secret configuration is stored in
.Pa ~/.config/piqley/config.json
as a JSON object.
The
.Cm setup
command creates and populates this file interactively.
Use
.Cm config
to open it directly in your editor.
.Bl -tag -width Ds
.It autoDiscoverPlugins
.Pq boolean, default: true
When enabled, any plugin directory found under
.Pa ~/.config/piqley/plugins/
with a valid
.Pa manifest.json
is loaded automatically.
.It disabledPlugins
.Pq array of strings, default: []
Plugin names to exclude from discovery and pipeline execution.
.It pipeline
.Pq object, default: {}
Maps hook names to ordered arrays of plugin names.
Controls the order in which plugins execute for each hook stage.
.El
.Ss Plugin configuration
Each plugin has its own
.Pa config.json
inside its plugin directory.
This file contains:
.Bl -tag -width Ds
.It values
Key-value settings the plugin reads at runtime (e.g., output quality,
tag prefixes).
.It rules
Declarative metadata-matching rules.
Each rule matches a field from the original image metadata or a
dependency plugin's output using exact, glob, or regex patterns, and
emits tags or keywords.
Rules can be scoped to specific hook stages.
.It isSetUp
Boolean flag set by
.Cm plugin setup
to indicate the plugin has been configured.
.El
.Sh PLUGIN ARCHITECTURE
Plugins live under
.Pa ~/.config/piqley/plugins/<name>/ .
Each plugin directory contains:
.Bl -tag -width Ds
.It Pa manifest.json
Declares the plugin's name, protocol version, version, config schema,
dependencies, and hook entries.
Each hook entry may specify an executable command with arguments and a
timeout.
.It Pa config.json
Runtime configuration: key-value settings and declarative rules.
Created by
.Cm plugin init
or
.Cm plugin setup .
.It Pa logs/execution.jsonl
Append-only execution log written by the pipeline orchestrator.
Cleared by
.Cm clear-cache .
.It Pa data/
Plugin-managed persistent data directory.
.El
.Ss Hooks
Plugins subscribe to named hooks declared in their manifest.
Hooks run in the order defined by the pipeline configuration.
A hook entry without a command is declarative-only and relies on the
rules in the plugin's
.Pa config.json .
.Ss Secrets
Plugin secrets are managed with
.Cm secret set
and
.Cm secret delete .
On macOS, secrets are stored in the Keychain under the
.Nm
service name.
On Linux, they are stored in
.Pa ~/.config/piqley/secrets.json .
.Sh FILES
.Bl -tag -width Ds
.It Pa ~/.config/piqley/config.json
Main configuration file (JSON).
Created and populated by the
.Cm setup
command.
.It Pa ~/.config/piqley/plugins/
Plugin directory.
Each subdirectory is a plugin containing
.Pa manifest.json
and optionally
.Pa config.json .
.It Pa ~/.config/piqley/secrets.json
Secret store for Linux.
On macOS, secrets are stored in the Keychain instead.
.It Pa ~/.config/piqley/piqley.lock
Advisory lock file to prevent concurrent execution.
.El
.Sh ENVIRONMENT
.Bl -tag -width Ds
.It Ev EDITOR
Editor used by
.Cm config
and
.Cm plugin config
to open JSON files.
Falls back to
.Xr open 1
on macOS when unset.
.El
.Sh EXIT STATUS
.Bl -tag -width Ds
.It 0
All images processed successfully (or command completed without error).
.It 1
Fatal error.
Causes include: missing or invalid configuration, plugin failures, and
another instance of
.Nm
already running.
.El
.Sh EXAMPLES
Process a folder of exported images:
.Bd -literal -offset indent
piqley process ~/Photos/export
.Ed
.Pp
Preview what would happen without uploading:
.Bd -literal -offset indent
piqley process ~/Photos/export --dry-run
.Ed
.Pp
Open the main config file in your editor:
.Bd -literal -offset indent
piqley config
.Ed
.Pp
Open a plugin's config file:
.Bd -literal -offset indent
piqley plugin config my-tagger
.Ed
.Pp
Create a new plugin with example rules:
.Bd -literal -offset indent
piqley plugin init my-tagger
.Ed
.Pp
Store a secret for a plugin:
.Bd -literal -offset indent
piqley secret set ghost api-key
.Ed
.Pp
Run setup for a specific plugin:
.Bd -literal -offset indent
piqley plugin setup ghost
.Ed
.Pp
Clear execution logs for a specific plugin:
.Bd -literal -offset indent
piqley clear-cache --plugin ghost
.Ed
.Pp
Remove all piqley data without confirmation:
.Bd -literal -offset indent
piqley uninstall --force
.Ed
